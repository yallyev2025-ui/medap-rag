"""Генерация ответов: главный промпт, проверка заземления и вызов модели.

Модель под задачу выбирает TaskModelMap (`app/llm/task_map.py`), а сам вызов идёт
через общий слой провайдеров — там же считается расход по §59 ТЗ.
"""

import logging
import re
from collections import Counter
from dataclasses import dataclass
from typing import Awaitable, Callable, Literal

from app.llm import provider as llm
from app.llm.task_map import Task
from app.verification.verify import verify_answer
from config import settings
from constants import SOURCE_CLINREK, SOURCE_TEXTBOOK
from rag.retriever import ChunkResult

logger = logging.getLogger(__name__)

NO_CONTEXT_ANSWER = (
    "Этой информации нет в материалах MedAP.\n"
    "Попробуй переформулировать вопрос."
)

NO_CONTEXT_PLACEHOLDER = "(в базе MedAP не найдено материалов, релевантных вопросу)"


def _is_relevant(chunk: ChunkResult) -> bool:
    """Релевантен ли фрагмент. Основной сигнал — скор реранкера; если реранкер был
    недоступен (rerank_score=None) — падаем на запасной косинусный порог."""
    if chunk.rerank_score is not None:
        return chunk.rerank_score >= settings.RERANK_SCORE_THRESHOLD
    return chunk.distance <= settings.MAX_DISTANCE_THRESHOLD


def relevant_chunks(chunks: list[ChunkResult]) -> list[ChunkResult]:
    return [c for c in chunks if _is_relevant(c)]

SYSTEM_PROMPT = f"""Ты — медицинский ассистент-бот MedAP. Помогаешь студентам медицинских вузов разбираться в учебном материале.

КАК ОПРЕДЕЛИТЬ ТИП СООБЩЕНИЯ:
1. Общее сообщение (приветствие, благодарность, вопрос "что ты умеешь", small talk) — отвечай как обычный дружелюбный ассистент, кратко, при первом обращении представься как MedAP.
2. Учебный/медицинский вопрос — действуй по АБСОЛЮТНОМУ ПРАВИЛУ ниже.

АБСОЛЮТНОЕ ПРАВИЛО для учебных вопросов:
Отвечай ТОЛЬКО на основе контекста из учебников, который передан тебе ниже. Никаких других источников. Никогда.

Если в контексте нет ответа на учебный вопрос (или указано, что релевантных материалов не найдено) — отвечай ровно:
"{NO_CONTEXT_ANSWER}"
Не дополняй и не угадывай.

ВАЖНО: упоминание темы в контексте — это НЕ ответ. Если в контексте встречается только название темы (например, строка из оглавления, заголовок раздела, список терминов без объяснения), а развёрнутого определения/механизма/ответа на вопрос там нет — это считается "ответа нет в контексте", даже если тема там названа. В таком случае отвечай ровно "{NO_CONTEXT_ANSWER}" и НЕ указывай источник.

ЗАПРЕЩЕНО при ответе на учебные вопросы:
- Использовать знания не из контекста
- Додумывать и дополнять от себя
- Отвечать по памяти
- Говорить "обычно", "как правило", "известно что", "в целом"
- Достраивать определения, механизмы, классификации и т.п., если в контексте есть только название/упоминание темы без самого содержания
- Указывать источник, если фактический ответ из него не взят (наличие темы в оглавлении источника не считается ответом из него)

ОБЯЗАТЕЛЬНО при ответе на учебные вопросы:
- В конце указывай источник(и) ровно в том виде, в котором они даны в квадратных скобках перед фрагментами контекста (например, [Автор, Название учебника, стр. N] или [Автор, Название учебника, стр. N-M]). Не придумывай и не меняй номера страниц.
- Отвечать на русском языке
- Отвечать МАКСИМАЛЬНО подробно, как для подготовки к экзамену: выжимай из контекста ВСЮ относящуюся к вопросу информацию до последней детали. Раскрывай определение, причины/этиологию, механизмы/патогенез, стадии, классификации, проявления, осложнения, исходы — всё, что есть в контексте по теме. Приводи конкретику из контекста: названия, цифры, примеры, термины. Не сокращай, не обобщай и не пропускай ни одной детали, которая есть в контексте.
- При этом без воды и канцеляризмов: каждое предложение должно нести конкретный факт из контекста, а не общие фразы. Подробность = больше фактов из материала, а НЕ больше общих слов.
- Перед тем как выдать финальный ответ, обязательно перечитай контекст и проверь себя: раскрыты ли ВСЕ части вопроса и использована ли КАЖДАЯ относящаяся к вопросу деталь из контекста? Если в контексте осталась хоть какая-то значимая информация по вопросу, которую ты не включил — дополни ответ ей перед выводом

РАБОТА С НЕСКОЛЬКИМИ ИСТОЧНИКАМИ:
В контексте могут быть фрагменты из разных учебников по одной теме. Это нормально и ожидаемо.
- Совмещай информацию из всех фрагментов в единый связный ответ по смыслу (определение, этиология, патогенез и т.д.), а не пересказывай каждый источник отдельным блоком "по Автору1... по Автору2..."
- Если источники дополняют друг друга (один даёт то, чего нет у другого) — объединяй без потери деталей ни одного из них
- Если одна и та же информация повторяется в нескольких источниках — пиши один раз, не дублируй
- Если источники ПРОТИВОРЕЧАТ друг другу (разные цифры, классификации, формулировки определения и т.п.) — явно укажи это: приведи обе версии и сошлись на источник каждой (например, "по [Автор1, ...] — ..., а по [Автор2, ...] — ...")
- В конце укажи ВСЕ источники, которые реально использовал в ответе (каждый фрагмент, чьи факты вошли в ответ), а не только один

ФОРМАТИРОВАНИЕ ДЛЯ TELEGRAM:
- Используй Markdown: **жирный** для терминов и заголовков, "-" для списков
- Не используй заголовки через "#" — только **жирный**
- Структурируй развёрнутый ответ по смысловым блокам (определение, этиология, патогенез, стадии и т.д.), используй списки и подзаголовки **жирным**. Полнота важнее краткости, но не повторяйся."""

# --- Клинический промпт для врачей (режим «Клин. рекомендации») ---
# Отличия от студенческого: профессиональный тон, клиническая структура ответа,
# ответ строго из ОДНОЙ рекомендации (мешанина недопустима), акцент на тактике.
CLINREK_SYSTEM_PROMPT = f"""Ты — медицинский ассистент-бот MedAP для практикующих врачей. Отвечаешь строго по официальным клиническим рекомендациям, фрагменты которых переданы в контексте ниже.

КАК ОПРЕДЕЛИТЬ ТИП СООБЩЕНИЯ:
1. Приветствие/благодарность/small talk — ответь коротко и по-деловому.
2. Клинический вопрос — действуй по правилам ниже.

АБСОЛЮТНОЕ ПРАВИЛО:
Отвечай ТОЛЬКО на основе контекста из клинических рекомендаций ниже. Никаких сведений из общих знаний, памяти или других источников. Если конкретной детали (дозы, срока, критерия) в контексте нет — так и напиши: «в этой рекомендации не указано», НЕ додумывай.

Если в контексте нет ответа на вопрос (или указано, что релевантных материалов не найдено) — ответь ровно:
"{NO_CONTEXT_ANSWER}"

ГЛУБИНА И ТОЧНОСТЬ (это для врача на приёме, а не для общего понимания):
- Отвечай развёрнуто и клинически конкретно: приводи из контекста точные критерии, классификации, стадии/степени, дозы препаратов, схемы и сроки, целевые показатели, показания/противопоказания — дословно, с цифрами.
- Держись сути вопроса: если спросили про лечение — раскрой тактику и препараты; если про диагностику — критерии и обследования. Не пересказывай всю рекомендацию, если спрашивают об одном.
- Где это уместно и есть в контексте, структурируй по клинической логике: **Определение/критерии**, **Диагностика**, **Классификация/степень тяжести**, **Лечение (тактика, препараты, дозы)**, **Наблюдение/критерии эффективности**, **Осложнения/прогноз**. Пункты, которых нет в контексте, просто опусти.
- Каждое утверждение — конкретный факт из рекомендации, без общих слов и «воды».

ОДНА РЕКОМЕНДАЦИЯ:
Контекст собран из ОДНОЙ клинической рекомендации по теме вопроса. Отвечай по ней. Не приплетай данные из смежных тем.

ИСТОЧНИК:
В конце укажи источник ровно в том виде, как он дан в квадратных скобках перед фрагментами (например, [Название рекомендации, стр. N]). Номера страниц не выдумывай.

ФОРМАТ (Telegram): **жирный** для заголовков и ключевых терминов, списки через «-», без «#». Отвечай на русском."""

# Клиническая инструкция режима — подставляется вместо студенческой для клинреков.
CLINREK_MODE_INSTRUCTION = (
    "Дай глубокий, клинически точный ответ для врача — с конкретикой из рекомендации "
    "(критерии, дозы, схемы, сроки, целевые показатели). Раскрой именно то, о чём "
    "спрашивают, по клинической структуре, опуская неприменимые пункты."
)

# --- Гибкий фолбэк: приветствие ИЛИ общий ответ, когда в материалах ничего нет ---
# Всегда честно помечает, что ответ не из загруженных материалов (требование продукта).
FALLBACK_SYSTEM_PROMPT = """Ты — медицинский ассистент MedAP.

1. Если сообщение — приветствие, благодарность, small talk или вопрос «что ты умеешь» — ответь дружелюбно и кратко, при первом обращении представься как MedAP. Без всяких пометок.

2. Если это медицинский/учебный вопрос, а в загруженных материалах MedAP по нему ничего не нашлось — ответь из своих ОБЩИХ знаний, соблюдая правила:
   - ОБЯЗАТЕЛЬНО начни ответ с этой строки (дословно, отдельным абзацем):
     "⚠️ В загруженных материалах MedAP этого нет — отвечаю из общих знаний ИИ. Это не официальный источник, перепроверьте."
   - Отвечай по объёму вопроса: короткий вопрос — короткий ответ, не раздувай и не трать лишние слова.
   - Ты даёшь информацию, а не назначение; окончательное решение и ответственность — на пользователе.
   - Не выдумывай точные дозировки/цифры, если не уверен — обозначь общий принцип и посоветуй свериться с первоисточником/клиническими рекомендациями.

На русском. Форматирование Telegram: **жирный**, списки через «-», без «#»."""

FALLBACK_USER_TEMPLATE = "Сообщение пользователя: {question}"

# --- Роутер: определяет тип клинического запроса, чтобы выбрать стратегию поиска ---
INTENT_SYSTEM_PROMPT = """Определи тип медицинского запроса врача и выведи РОВНО одно слово-код:

- SINGLE — вопрос про одну конкретную болезнь/препарат/тему (что это, диагностика, лечение чего-то НАЗВАННОГО по имени).
- DIFFERENTIAL — описаны симптомы/жалобы/клиническая картина БЕЗ готового диагноза, нужно предположить, что это может быть (дифференциальный диагноз).
- MULTI — несколько заболеваний/состояний сразу, их сочетание или последовательность, нужен совместный разбор/тактика.
- CHITCHAT — приветствие, благодарность, «что ты умеешь», не медицинский вопрос.

Выведи только одно: SINGLE, DIFFERENTIAL, MULTI или CHITCHAT."""


async def detect_intent(question: str) -> str:
    """Классифицирует запрос (роутер использует обычную/дешёвую модель).
    При сбое — безопасный дефолт SINGLE."""
    try:
        out = await _complete(INTENT_SYSTEM_PROMPT, question, 0.0, Task.INTENT_ROUTER)
    except RuntimeError:
        return "SINGLE"
    up = out.strip().upper()
    for key in ("DIFFERENTIAL", "MULTI", "CHITCHAT", "SINGLE"):
        if key in up:
            return key
    return "SINGLE"


# --- Лёгкая память диалога ---
# Сколько символов ответа держим в истории (полные ответы раздувают промпт).
HISTORY_ANSWER_CHARS = 800

REWRITE_SYSTEM_PROMPT = """Переформулируй ПОСЛЕДНИЙ вопрос пользователя в самостоятельный поисковый запрос с учётом диалога — так, чтобы он был понятен без предыдущих сообщений (раскрой отсылки вроде «а дозы?», «при нём», «это» в конкретную тему из диалога). Сохрани медицинские термины.
Выведи ТОЛЬКО переформулированный запрос, одной строкой, без пояснений. Если вопрос и так самостоятельный — верни его без изменений."""


async def rewrite_query(question: str, turns: list[tuple[str, str]]) -> str:
    """Уточняющий вопрос («а какие дозы?») превращает в самостоятельный запрос для
    поиска, используя историю диалога. Без истории возвращает вопрос как есть."""
    if not turns:
        return question
    hist = "\n".join(f"Вопрос: {q}\nОтвет: {a[:300]}" for q, a in turns)
    try:
        out = await _complete(
            REWRITE_SYSTEM_PROMPT,
            f"Диалог:\n{hist}\n\nПоследний вопрос: {question}",
            0.0,
            Task.QUERY_REWRITE,
        )
    except RuntimeError:
        return question
    return out.strip() or question


def build_history_messages(turns: list[tuple[str, str]]) -> list[dict]:
    """Готовит историю диалога в формате сообщений для модели (ответы усечены)."""
    messages: list[dict] = []
    for q, a in turns:
        messages.append({"role": "user", "content": q})
        messages.append({"role": "assistant", "content": a[:HISTORY_ANSWER_CHARS]})
    return messages


# --- Дифференциальный диагноз по симптомам (много рекомендаций, строго по контексту) ---
DIFFERENTIAL_SYSTEM_PROMPT = """Ты — клинический ассистент MedAP для врача. Тебе даны фрагменты официальных клинических рекомендаций (КОНТЕКСТ) и описание симптомов/картины пациента.
Проведи дифференциально-диагностический разбор СТРОГО на основе контекста.

КРИТИЧНО (медицина, ошибки опасны, галлюцинации недопустимы):
- Рассматривай ТОЛЬКО те заболевания/состояния, которые есть в контексте и к которым подходят описанные признаки. НЕ придумывай болезни, которых нет в контексте.
- «За»/«против» для каждой версии — ТОЛЬКО по признакам/критериям из контекста. Чего нет в контексте — не утверждай.
- Не ставь диагноз. Формулируй как «стоит рассмотреть / требует исключения».
- Если симптомы не сопоставляются ни с чем в контексте — честно скажи об этом, не выдумывай.

ФОРМАТ:
1. Кратко: картина пациента (1–2 строки, из слов пользователя).
2. **Вероятные версии** — по убыванию вероятности по данным контекста. По каждой: «за» / «против» (из рекомендаций) и источник [Название, стр. N].
3. **Что уточнить / обследовать** — какие данные помогут различить (если есть в контексте).
4. **🚩 Красные флаги** — что срочно исключить (если есть в контексте).
5. Короткий дисклеймер: это ориентир по загруженным рекомендациям, не диагноз; нужен очный осмотр, решение — за врачом.

Русский, Telegram-Markdown (**жирный**, списки «-», без «#")."""

# --- Разбор сочетания/последовательности заболеваний (много рекомендаций) ---
MULTI_SYSTEM_PROMPT = """Ты — клинический ассистент MedAP для врача. В КОНТЕКСТЕ — фрагменты официальных клинических рекомендаций по нескольким состояниям. Запрос касается их сочетания или последовательности.

СТРОГО по контексту, без выдумок (галлюцинации недопустимы):
- Разбери каждое состояние и как их вести ВМЕСТЕ: приоритеты, противоречия в терапии, лекарственные взаимодействия, взаимное влияние — но только если это есть в контексте.
- Чего нет в контексте — не утверждай, честно отметь пробел.
- Не назначение, а информация из рекомендаций; решение — за врачом.

ФОРМАТ: связный разбор по состояниям и их сочетанию, что контролировать, источники [Название, стр. N] по блокам, короткий дисклеймер. Русский, Telegram-Markdown."""

REASONING_USER_TEMPLATE = """Контекст из клинических рекомендаций:
{context}

Запрос врача: {question}"""

MODE_INSTRUCTIONS = {
    "question": (
        "Дай максимально подробный и исчерпывающий ответ — на уровне подготовки к экзамену. "
        "Раскрой тему со всех сторон, опираясь на контекст: определение, причины, "
        "механизмы, стадии, классификации, проявления, осложнения, исходы — всё, что есть "
        "в контексте по теме. Используй весь относящийся к вопросу материал из контекста, "
        "не ограничивайся одним абзацем."
    ),
    "conspect": "Сделай структурированный конспект по теме: заголовки **жирным**, пункты списком, ключевые определения.",
    "explanation": "Объясни простыми словами, используй аналогии и примеры ТОЛЬКО из контекста ниже.",
}

# Шаблоны структуры ответа по предметам (subject из db/models.Book.subject,
# совпадает с кодами из bot/handlers/admin.SUBJECTS). Подставляются один раз
# в промпт по предмету найденных чанков — не генерируются заново на каждый вопрос.
SUBJECT_STRUCTURES: dict[str, tuple[str, list[str]]] = {
    "pathanatomy": (
        "патанатомии",
        [
            "Определение",
            "Классификация (виды/формы)",
            "Этиология",
            "Патогенез",
            "Морфологические изменения (макро- и микроскопическая картина)",
            "Осложнения и исходы",
        ],
    ),
    "pathphys": (
        "патофизиологии",
        [
            "Определение",
            "Этиология",
            "Патогенез (стадии, механизмы)",
            "Виды/классификация",
            "Клинические проявления и их механизмы",
            "Исходы и значение для организма",
        ],
    ),
    "physiology": (
        "физиологии",
        [
            "Определение и функция",
            "Анатомический субстрат (где происходит)",
            "Механизм процесса",
            "Регуляция",
            "Значение для организма",
        ],
    ),
    "anatomy": (
        "анатомии",
        [
            "Определение и расположение",
            "Классификация (виды/типы)",
            "Строение (отделы/слои/части)",
            "Кровоснабжение и иннервация (если применимо)",
            "Функция",
            "Топографические соотношения с соседними структурами",
        ],
    ),
    "biochemistry": (
        "биохимии",
        [
            "Определение/структура вещества или процесса",
            "Классификация (класс/группа)",
            "Локализация в клетке/организме",
            "Этапы и реакции (с ключевыми ферментами)",
            "Регуляция",
            "Биологическое значение",
        ],
    ),
    "pharmacology": (
        "фармакологии",
        [
            "Группа и классификация препарата",
            "Механизм действия",
            "Фармакологические эффекты",
            "Показания к применению",
            "Побочные эффекты и противопоказания",
        ],
    ),
}


def _structure_hint(subject: str | None) -> str:
    """Подсказка по структуре ответа для предмета. Пустая строка, если для
    предмета нет шаблона (например, "other" или кастомный предмет)."""
    entry = SUBJECT_STRUCTURES.get(subject or "")
    if entry is None:
        return ""

    subject_name, sections = entry
    sections_text = "\n".join(f"- {s}" for s in sections)
    return (
        f"\n\nЕсли вопрос — про конкретную нозологию/процесс/тему по {subject_name} "
        f"и в контексте достаточно материала, структурируй ответ по схеме:\n{sections_text}\n"
        "Если вопрос не укладывается в эту схему (общий вопрос, термин, сравнение, "
        "вопрос не по этой теме и т.п.) или в контексте нет материала для каких-то пунктов — "
        "не подгоняй искусственно: просто ответь по существу вопроса, опустив неприменимые пункты."
    )


GENERATION_TEMPERATURE = 0.2

USER_PROMPT_TEMPLATE = """{mode_instruction}

Контекст из {source_label}:
{context}

Вопрос: {question}

Напоминание: используй ТОЛЬКО контекст выше. Если ответа в контексте нет — ответь ровно "{no_context_answer}", без пояснений и догадок."""

# --- Проверочный проход (groundedness) — см. app/verification/verify.py -----

CORRECTION_TEMPLATE = """{mode_instruction}

Контекст из {source_label}:
{context}

Вопрос студента: {question}

Твой предыдущий ответ содержал утверждения, которых НЕТ в контексте:
{issues}

Перепиши ответ, оставив ТОЛЬКО то, что напрямую подтверждается контекстом выше. \
Если после удаления неподтверждённого не остаётся содержательного ответа — выведи ровно "{no_context_answer}"."""

CONSPECT_PATTERN = re.compile(
    r"конспект|кратко разбери|структурируй|выпиши основное", re.IGNORECASE
)
EXPLANATION_PATTERN = re.compile(
    r"объясни|простыми словами|по-простому|на пальцах|аналоги", re.IGNORECASE
)


def detect_mode(question: str) -> Literal["question", "conspect", "explanation"]:
    if CONSPECT_PATTERN.search(question):
        return "conspect"
    if EXPLANATION_PATTERN.search(question):
        return "explanation"
    return "question"


def _format_source(chunk: ChunkResult) -> str:
    # У клинреков автора нет (author пустой) — источником служит название рекомендации.
    head = f"{chunk.author}, {chunk.title}" if chunk.author else chunk.title
    if chunk.page_from is None:
        return head
    if chunk.page_from == chunk.page_to:
        return f"{head}, стр. {chunk.page_from}"
    return f"{head}, стр. {chunk.page_from}-{chunk.page_to}"


def build_context(chunks: list[ChunkResult]) -> str:
    relevant = relevant_chunks(chunks)
    if not relevant:
        return NO_CONTEXT_PLACEHOLDER
    return "\n---\n".join(f"[{_format_source(c)}]\n{c.content}" for c in relevant)


def detect_subject(chunks: list[ChunkResult]) -> str | None:
    relevant = relevant_chunks(chunks)
    if not relevant:
        return None
    return Counter(c.subject for c in relevant).most_common(1)[0][0]


async def _complete(
    system_prompt: str,
    user_prompt: str,
    temperature: float,
    task: Task = Task.GROUNDED_QA,
    history: list[dict] | None = None,
) -> str:
    """Вызов модели через общий слой провайдеров.

    Модель не задаётся здесь: её определяет TaskModelMap по атомарной задаче
    (§56.5 ТЗ), а слой провайдера сохраняет расход по каждому вызову (§59).
    """
    messages = [{"role": "system", "content": system_prompt}]
    if history:
        messages.extend(history)
    messages.append({"role": "user", "content": user_prompt})
    try:
        result = await llm.complete(task, messages, temperature=temperature)
    except llm.LLMError as e:
        raise RuntimeError(llm.PROVIDER_ERROR_MESSAGE) from e
    return result.text


@dataclass
class GeneratedAnswer:
    text: str
    # True — прошёл проверку (Verification Layer). False — не прошёл даже после
    # корректирующей перегенерации, текст уже заменён на честный отказ (§15).
    # None — верификация не выполнялась (VERIFY_GROUNDING=False) или сам
    # верификатор был недоступен (§36) — ответ не помечается verified.
    verified: bool | None


async def _verify_and_repair(
    context: str,
    answer: str,
    correct: Callable[[str], Awaitable[str]],
) -> GeneratedAnswer:
    """Общий цикл Verification Layer для generate_answer/_reasoning_answer
    (app/verification/verify.py): PASS сразу; при проблеме — один корректирующий
    проход (`correct(issues)`) и повторная проверка; если проблема осталась —
    честный отказ вместо недостоверного ответа, а не молчаливая выдача как есть.
    """
    if not settings.VERIFY_GROUNDING:
        return GeneratedAnswer(answer, verified=None)

    result = await verify_answer(context, answer)
    if result.grounded is True:
        return GeneratedAnswer(answer, verified=True)
    if result.grounded is None:
        return GeneratedAnswer(answer, verified=None)

    logger.info("Ответ не прошёл проверку, перегенерирую. Проблемы: %s", result.issues)
    corrected = await correct(result.issues)

    result2 = await verify_answer(context, corrected)
    if result2.grounded is True:
        return GeneratedAnswer(corrected, verified=True)
    if result2.grounded is None:
        return GeneratedAnswer(corrected, verified=None)

    logger.info("Исправленный ответ снова не прошёл проверку — честный отказ вместо недостоверного ответа")
    return GeneratedAnswer(NO_CONTEXT_ANSWER, verified=False)


async def generate_answer(
    question: str,
    chunks: list[ChunkResult],
    source_type: str = SOURCE_TEXTBOOK,
    history: list[dict] | None = None,
) -> GeneratedAnswer:
    """Генерирует ответ с учётом режима.

    - source_type='клинрек' — врачебный клинический промпт (глубоко, из одной рекомендации).
    - source_type='учебник' — студенческий промпт (как для экзамена).
    Если в материалах релевантного нет — гибкий фолбэк: приветствие или ответ из общих
    знаний ИИ с ОБЯЗАТЕЛЬНОЙ пометкой, что это не из загруженных материалов.
    """
    is_clinrek = source_type == SOURCE_CLINREK
    source_label = "клинических рекомендаций" if is_clinrek else "учебников"
    context = build_context(chunks)

    # Материалов по вопросу нет: либо приветствие/small talk, либо общий вопрос —
    # уводим в гибкий фолбэк (общие знания с честной пометкой). Верификация не нужна.
    if context == NO_CONTEXT_PLACEHOLDER:
        answer = await _complete(
            FALLBACK_SYSTEM_PROMPT,
            FALLBACK_USER_TEMPLATE.format(question=question),
            GENERATION_TEMPERATURE,
            history=history,
        )
        return GeneratedAnswer(answer, verified=None)

    system_prompt = CLINREK_SYSTEM_PROMPT if is_clinrek else SYSTEM_PROMPT
    mode = detect_mode(question)

    if is_clinrek:
        mode_instruction = CLINREK_MODE_INSTRUCTION
    else:
        mode_instruction = MODE_INSTRUCTIONS[mode]
        if mode in ("question", "conspect"):
            mode_instruction += _structure_hint(detect_subject(chunks))

    user_prompt = USER_PROMPT_TEMPLATE.format(
        mode_instruction=mode_instruction,
        source_label=source_label,
        context=context,
        question=question,
        no_context_answer=NO_CONTEXT_ANSWER,
    )

    answer = await _complete(
        system_prompt, user_prompt, GENERATION_TEMPERATURE, Task.GROUNDED_QA, history
    )

    async def correct(issues: str) -> str:
        correction_prompt = CORRECTION_TEMPLATE.format(
            mode_instruction=mode_instruction,
            source_label=source_label,
            context=context,
            question=question,
            issues=issues,
            no_context_answer=NO_CONTEXT_ANSWER,
        )
        return await _complete(
            system_prompt, correction_prompt, GENERATION_TEMPERATURE, Task.GROUNDED_QA, history
        )

    return await _verify_and_repair(context, answer, correct)


async def _reasoning_answer(
    system_prompt: str,
    question: str,
    chunks: list[ChunkResult],
    source_type: str,
    history: list[dict] | None = None,
) -> GeneratedAnswer | None:
    """Заземлённый клинический разбор (дифдиагноз/сочетание). Возвращает None, если
    релевантных материалов нет — тогда вызывающий спросит согласие на общие знания."""
    context = build_context(chunks)
    if context == NO_CONTEXT_PLACEHOLDER:
        return None

    user_prompt = REASONING_USER_TEMPLATE.format(context=context, question=question)
    answer = await _complete(
        system_prompt, user_prompt, GENERATION_TEMPERATURE, Task.GROUNDED_QA, history
    )

    async def correct(issues: str) -> str:
        correction = user_prompt + (
            "\n\nВАЖНО: убери из ответа ВСЕ утверждения, которых нет в контексте выше "
            "(медицина — выдумки недопустимы). Проблемные места:\n" + issues
        )
        return await _complete(
            system_prompt, correction, GENERATION_TEMPERATURE, Task.GROUNDED_QA, history
        )

    return await _verify_and_repair(context, answer, correct)


async def generate_differential(
    question: str,
    chunks: list[ChunkResult],
    source_type: str = SOURCE_CLINREK,
    history: list[dict] | None = None,
) -> GeneratedAnswer | None:
    """Дифференциальный диагноз по симптомам (строго по многим рекомендациям)."""
    return await _reasoning_answer(DIFFERENTIAL_SYSTEM_PROMPT, question, chunks, source_type, history)


async def generate_multi(
    question: str,
    chunks: list[ChunkResult],
    source_type: str = SOURCE_CLINREK,
    history: list[dict] | None = None,
) -> GeneratedAnswer | None:
    """Разбор сочетания/последовательности заболеваний (по многим рекомендациям)."""
    return await _reasoning_answer(MULTI_SYSTEM_PROMPT, question, chunks, source_type, history)


async def generate_fallback(question: str) -> str:
    """Ответ из общих знаний ИИ с обязательной пометкой (по согласию пользователя),
    либо приветствие/small talk. Модель обычная — общие знания, экономим."""
    return await _complete(
        FALLBACK_SYSTEM_PROMPT,
        FALLBACK_USER_TEMPLATE.format(question=question),
        GENERATION_TEMPERATURE,
        Task.GROUNDED_QA,
    )
